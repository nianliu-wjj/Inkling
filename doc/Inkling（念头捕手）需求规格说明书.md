# Inkling（念头捕手）需求规格说明书

> **文档版本**：v1.2（含交互优化第一轮：悬浮显隐/热力图重做/单窗口归档/搜索/优先级阶梯/逾期置顶；第二轮：完成时间模型/标签/备注/侧边栏折叠等）
> **编写日期**：2026-08-29
> **审查基线**：2026-08-29
> **文档状态**：审查中，需求冻结前需完成本文件与架构文档的口径统一
> **原型边界**：`doc/index.html` 是静态交互演示，不代表真实 SQLite、系统剪贴板、托盘、窗口层级、跨平台权限或持久化已经实现；差异见《文档审查与风险清单》。
> **文档存放**：`Inkling/doc/Inkling（念头捕手）需求规格说明书.md`

---

## 0. 版本变更摘要

### v1.1 → v1.2 变更清单

| # | 变更点 | 说明 |
| :- | :--- | :--- |
| 1 | **卡片悬浮显隐** | 所有卡片（笔记/粘贴板/待办，含面板与历史归档）的 ✕ 关闭按钮置于右上角，功能按钮（编辑/置顶/收藏/粘贴/提醒/子任务等）置于底部右侧，均仅在鼠标悬浮当前卡片时显示；待办与子任务按钮严格层级隔离（悬浮父级不显示子任务按钮，反之亦然） |
| 2 | **呼出面板层级** | 呼出面板置于所有窗口之上（含历史归档主窗口、统计、设置） |
| 3 | **热力图重做** | 日历格子热力图（列=周、行=星期），顶部标注每个月范围；悬浮显示日期与明细（笔记条数、复制项条数、待办条数及已完成/逾期数），存在逾期的日期格子以红色边框标识；趋势图改为折线图；统计页支持滚动 |
| 4 | **归档单窗口** | 历史归档改为左右结构单窗口：左侧边栏（笔记/粘贴板/待办页签，底部左 ⚙️ 偏好设置、右 📊 统计），点击均在右侧主内容区展示，不再弹独立窗口 |
| 5 | **侧边栏折叠/拖宽** | 侧边栏支持拖动分隔条实时调宽（110~280px）；拖至阈值以下自动折叠为图标窄栏（52px，仅图标可点）；点击 » 恢复默认宽度 150px |
| 6 | **优先级阶梯菜单** | 点击待办优先级徽章弹出阶梯下拉：当前优先级由 Pin 卡片本身充当（高亮描边）——「低」浮层在卡片上方（高→中）、「中」高在上低在下、「高」浮层在下方（中→低）；三色区分；紧贴卡片内容行定位 |
| 7 | **逾期置顶** | 逾期事项归入当日列表顶部「逾期事项」分区，按完成时间、优先级排序；父待办存在逾期子任务时整体（含已完成子任务）归入；已完成项不可修改 |
| 8 | **删除二次确认** | 所有卡片的 ✕ 均需二次确认；确认框**悬浮于卡片上方**（不占满卡片、不推挤布局） |
| 9 | **粘贴板两行省略** | 粘贴板卡片内容最多展示两行，超出省略号截断（面板与归档一致） |
| 10 | **待办标签** | 待办/子任务支持标签：最多 3 个、每个 ≤10 字、去重；编辑弹窗内增删（✕ 抖动二次确认）；卡片左下角以 chip 展示 |
| 11 | **待办备注** | 待办/子任务支持备注（≤200 字，编辑弹窗内带字数计数）；卡片展示样式三选一（设置页配置）：图标徽章+悬浮 / 置灰文本行 / 混合模式（默认：≤100 字文本行，>100 字图标） |
| 12 | **完成时间模型** | 待办/子任务新增「完成时间」（日期+时刻，与任务内容同为必填）；提醒时间改为选填（完整日期+时间）；排序、逾期均以完成时间为准 |
| 13 | **搜索** | 历史归档待办页日期行右侧新增搜索框与「＋新增待办」按钮；待办搜索跨全部日期（含子任务文本与已完成项，结果显示所属日期，命中子任务高亮）；笔记（正文+标签）与粘贴板同样支持搜索 |
| 14 | **其他** | 待办卡片不再显示「n/m 子任务」进度计数；全局 select 下拉选项跟随深色主题 |
| 15 | **完成时间徽章** | 完成时间以「📅」徽章常显于卡片底部标签之后（父/子任务均有，逾期红色），点击弹出「修改完成时间」聚焦弹窗（仅完成日期/时刻可编辑），保存后排序与逾期即时更新 |
| 16 | **子任务卡片同构** | 子任务与父级一致具备：右上角 ✕、底部左侧标签+完成时间徽章、备注展示（文本行/图标）、底部右侧操作区（⏰ 提醒 / ✏️ 编辑） |
| 17 | **笔记卡片元数据行** | 标签 chips 与时间**同一行**展示，紧跟在时间之后；卡片正文渲染 Markdown（粗体/代码等，与编辑器一致的轻量渲染），不再展示源码 |
| 18 | **已完成项完全禁改** | 已完成的待办/子任务不允许任何修改，**包括取消勾选恢复未完成**（点击 checkbox 拦截并 toast 提示）；例外：已完成的父待办仍可 **＋子任务**——新建未完成子任务后，系统自动将父待办恢复为未完成（默认完成时间 1 小时后，不受"不晚于父级"约束） |
| 19 | **单窗口品牌化** | 主窗口标题栏左侧固定展示项目名称与图标（✒️ Inkling），不再随视图显示"历史归档"等字样；系统托盘右键弹出 **Inkling 主菜单**（应用名作菜单标题 + 历史归档/统计报表/偏好设置/退出） |
| 20 | **折叠按钮入分界线** | 侧边栏折叠/展开按钮移至侧边栏与内容区**分界线的垂直居中处**，**默认隐藏**，鼠标悬浮侧边栏或分界线时显示 |
| 21 | **当月迷你热力图与日期详情** | 侧边栏底部嵌入**当月热力图**（真实业务数据按日统计，逾期日红框；“pin”仅是类别标识，不代表置顶）：悬浮显示当日明细；**点击某日 → 主内容区展示该日全部笔记/粘贴板/待办**，按时间先后排序（待办取计划完成时间），支持按类别（笔记、粘贴板、待办）筛选，默认使用不同颜色展示不同类别（笔记=蓝、粘贴板=黄、待办=绿） |
| 22 | **日期详情卡片增强** | 详情卡片展示**完整信息**：待办（优先级徽章、提醒时刻、重复规则、标签、备注、逾期标记、子任务归属）、笔记（Markdown 渲染正文、标签）、粘贴板（类型徽章、正文）；卡片支持**关键字查询**（匹配文本/标签/备注）、**编辑**（笔记回显主面板、粘贴板弹编辑浮框、待办弹编辑窗）与**删除**（悬浮 ✕ → 卡片上方确认框），增删改后热力图与各列表实时同步 |

---

## 1. 产品愿景与设计哲学

### 1.1 一句话定位
**Inkling** 是一款常驻系统状态栏、从屏幕顶部中央零延迟滑入的**极简桌面念头捕获工具（Fleet-moment Thought Catcher）**。

### 1.2 核心信条
- **1 秒原则**：从念头产生到文字落屏，时间必须 $< 1$ 秒，且**全程不切换当前工作应用**。
- **安静待命**：零红点、零自启弹窗、零打扰。
- **本地优先**：数据默认存放在本机，用户完全掌控隐私与资产。
- **跨平台一致性**：macOS 与 Windows 体验完全统一（Windows 下无视物理刘海差异，统一从屏幕顶部中央滑入）。

---

## 2. 核心功能需求（原子粒度拆解）

### 2.1 顶部中央悬浮展开（Core Interaction）
- **触发机制**：
  - **鼠标触顶**：光标移动至屏幕顶部中央（水平居中 $\pm 120\text{px}$，高度 $80\text{px}$ 隐形感应区）悬停超过 $100\text{ms}$ 自动滑出。
  - **全局快捷键**：默认 `Cmd/Ctrl + Shift + Space`（支持在设置中自定义录制）。
- **动效与尺寸**：
  - 展开/收启动效：引入专业动画组件库（如 GSAP / Motion One），呈现具有物理弹性（Spring/Ease-out）的流畅滑入（$200\text{ms}$）与滑出（$150\text{ms}$）过渡。
  - 窗口尺寸：固定宽度 $480\text{px}$，高度根据输入内容自适应（最小 $120\text{px}$，最大 $600\text{px}$）。
  - 视觉样式：无边框（Frameless）、半透明毛玻璃特效（macOS Vibrant / Windows Acrylic/Mica）、细微微光描边（$1\text{px}$ 半透明 Border）。
- **层级规范**：呼出面板**置于所有窗口之上**，包括历史归档主窗口（打开归档时呼出面板仍悬浮其上）。
- **失焦逻辑**：
  - 窗口失去焦点（Blur）时自动收起，支持配置：`立即收起` / `延迟 3 秒收起` / `固定不收起`。
  - **弹窗失焦保护**：当任何编辑弹窗（待办编辑弹窗、标签管理弹窗、剪贴板内容编辑浮框）处于打开状态时，呼出面板**不因失焦而自动收起**（弹窗保存后内容需回显至面板）；全部弹窗关闭后，若鼠标已不在面板区域内，则按配置的延迟策略重新计时收起。

### 2.2 顶部三态圆点切换（Tri-State Navigation）
顶部居中呈现 3 枚极简圆点指示器，支持键盘快捷键 `Cmd/Ctrl + 1/2/3` 或鼠标点击无缝切换：

1. 🔴 **笔记模式（Note Mode）**：
   - **Markdown 即时渲染**：编辑区域默认采用 Markdown 格式，具备 Typora 级别的即时渲染体验（所见即所得，光标移入展示语法标记，移出渲染最终效果）。
   - **归档交互**：支持通过点击界面的「归档/发送」按钮将内容归档为新念头，同时支持快捷键发送。
   - **混合存储策略**：
     - 未归档前，输入停止 $500\text{ms}$ 自动保存至本地 SQLite 数据库作为暂存。
     - 点击按钮归档时，支持主动落盘存储。
     - **超限自动落盘**：当笔记内容超过 **1MB** 时，在归档时自动落盘存储为 `.md` 文件，存放在应用数据目录的 `notes/` 中；不得默认写入项目安装目录或源码目录，SQLite 中仅保留索引与相对路径引用。
   - **标签体系（独立于正文的元数据）**：
     - **标签与正文解耦**：标签**不以 `#标签` 形式存在于文本内容中**；标签作为笔记的独立元数据维护。
     - **展示位置（仅两处）**：
       1. **历史归档页·笔记卡片元数据行**：标签 chips 与时间**同一行**展示，**紧跟在时间之后**；最多展示 **3 个**，超出以「+N」省略按钮聚合，点击展开全部；每个标签最多 **5 个字**，超出省略号截断，鼠标悬浮显示完整标签名。
       2. **呼出面板·笔记编辑区右下角**（归档按钮左侧）：新建时展示草稿标签，编辑已有笔记时展示该笔记标签；点击标签区弹出标签管理弹窗。
     - **卡片正文渲染**：笔记卡片正文按 Markdown 渲染展示（粗体/代码等，与编辑器一致的轻量渲染），不展示源码标记。
     - **无标签占位**：当笔记没有任何标签时，两处位置均显示置灰的「无标签」占位文本；占位**不可删除**（无 ✕ 按钮），点击时弹出标签管理页面。
     - **标签管理弹窗**：标签采用**流式布局**；支持**新增**（输入框回车，上限 5 字、去重）、**修改**（点击标签文字就地编辑）、**删除**标签。
     - **删除二次确认（卡片与管理弹窗一致）**：标签的 ✕ 删除按钮**仅在鼠标悬浮于该标签上时才显示**；首次点击 ✕ 进入「抖动确认态」——目标标签以约 **0.7s/次** 的频率左右抖动并变红，**鼠标悬浮到 ✕ 上时抖动动画立即暂停**；再次点击即删除；3 秒无操作自动退出抖动态。
   - **卡片按钮悬浮显隐（v1.2 统一规范，适用于全部卡片）**：
     - **✕ 删除按钮**：卡片**右上角**，仅占据一个角落位置，**仅鼠标悬浮当前卡片时显示**。
     - **功能按钮组**（置顶/编辑等）：卡片**底部右侧**，仅悬浮当前卡片时显示。
     - 点击 ✕ → 卡片**上方悬浮确认框**（「确认删除该笔记？/ 删除 / 取消」，绝对定位于卡片上方、不占满卡片、不推挤布局），二次确认后删除。

2. 🟡 **粘贴板历史（Clipboard Manager）**：
   - 自动监听系统剪贴板（文本、图片、URL、代码片段），所有数据统一持久化存储在 SQLite 中。
   - **数据保留策略**：最多保存最近 **30 天** 的数据，超出部分自动清理。
   - 列表展示最近捕获的历史条目，**需显示复制时间**；若内容被修改保存，则时间更新为最后一次修改保存的时间。
   - 支持实时模糊搜索（呼出面板与历史归档页均有）。
   - **内容截断**：粘贴板卡片内容**最多展示两行**，超出部分使用省略号截断（呼出面板与历史归档一致）。
   - **双击操作**：双击条目直接执行粘贴，并将该条目置顶。
   - **条目布局（呼出面板·粘贴板列表）**：
     - **左上角**：显示该条目的时间（复制时间 / 最后修改时间）。
     - **右上角**：✕ 删除图标按钮（仅悬浮卡片时显示；点击后**卡片上方悬浮二次确认框**，确认后删除）。
     - **右下角**：操作图标按钮组（仅悬浮卡片时显示）——**粘贴**（写回剪贴板）、**打开链接**（仅链接类型显示）、**编辑**（仅文本类条目显示）、**收藏/置顶**（Pin）。
   - **收藏与置顶**：支持单条 Pin 收藏与置顶管理；置顶条目金色高亮并优先排序。
   - **历史归档页粘贴板条目**：数据类型（文本/链接/代码/图片/富文本）以**彩色标签徽章**包裹展示；置顶条目优先排序并高亮描边；链接类型条目提供**「在默认浏览器中打开」图标按钮**；归档页同样提供搜索框。
   - **置顶（Pin）与编辑为两个独立功能**：置顶直接切换不弹编辑框；编辑弹出文本编辑浮框并回显，保存后替换原文本、时间更新为最后修改时间，不影响置顶状态；仅文本类条目可编辑。

3. 🟢 **待办事项（Quick Todo）**：
   - 所有待办数据统一存储在 SQLite 中；待办与子任务采用**标准树状结构**（一个待办最多 **5** 个子任务，子任务不可再嵌套）。
   - **字段模型（待办与子任务一致，v1.2）**：
     - **任务内容**（必填）。
     - **计划完成时间**（界面仍显示为「完成时间」，必填）：**完成日期 + 完成时刻**两个选择器；是列表归属、排序与逾期判定的唯一时间基准。它表示截止/计划完成时刻，不是用户实际勾选完成的时刻；实际完成时间另存为 `completed_at`。卡片底部以「📅」徽章**常显于标签之后**，点击可直接修改（专用聚焦弹窗）。
     - **提醒时间**（选填）：**完整日期 + 时间**（可早于/晚于完成日期）；留空时使用默认提醒计划（见「提醒」）。
     - **优先级**（高 🔴 / 中 🟡 / 低 🟢）。
     - **标签**（选填）：最多 **3 个**、每个最多 **10 字**、去重；在编辑弹窗内管理（输入回车添加，✕ 删除走抖动二次确认，规则同笔记标签）；卡片**左下角**以 chip 展示，点击 chip 进入编辑。
     - **备注**（选填）：最多 **200 字**；编辑弹窗内提供多行输入框与实时字数计数。卡片上的展示样式由偏好设置配置（见「备注展示样式」）。
   - **创建约束**：创建待办/子任务时，**完成时间不能早于当前时刻**（日期选择器下限锁定为今天，保存时二次校验）；**默认值 = 当前日期 + 1 小时后**。编辑/修改已有事项不设此下限（允许查看历史事项）；历史归档页在历史日期点「新增待办」时默认归入该日期并允许补录（提示「历史日期补录」）。
   - **子任务约束**：子任务的**完成时间不能晚于父待办的完成时间**（违规拦截提示）。
   - **编辑弹窗统一**：修改完成时间、提醒时间、编辑内容、标签、备注统一走该编辑弹窗（原生日期/时间选择器，非文本输入）；「提醒模式」下其余字段只读禁用，仅可修改提醒。
   - **优先级体系**：
     - 三级优先级以**不同颜色徽章**标识（高=红、中=黄、低=绿）。
     - **排序规则（v1.2）**：统一按**完成时间升序 → 优先级高在前**；**已完成的沉底**（仍按完成时间、优先级）。
     - **优先级阶梯菜单（v1.2）**：点击卡片上的优先级徽章弹出阶梯式下拉，**当前优先级由 Pin 卡片本身充当（按优先级色高亮描边），不重复出现在列表中**：
       - 当前为「低」：浮层**悬浮在卡片上方**，从上到下为「高」「中」；
       - 当前为「中」：「高」悬浮在卡片上方、「低」悬浮在卡片下方；
       - 当前为「高」：浮层悬浮在卡片**下方**，从上到下为「中」「低」。
       - 浮层紧贴卡片内容行定位（不跨过子任务树），防出屏自动翻转；每个选项以红/黄/绿圆点区分颜色；点击外部或 Esc 关闭；已完成待办不可修改优先级。
   - **逾期规则（v1.2，以完成时间为准）**：
     - 未完成且**完成时刻已过** → 逾期：当日列表中**完成时刻已过的当天事项**即时标红（无需等到次日）；查看历史日期时，该日未完成事项均视为逾期。
     - 逾期以**醒目红边+红底+「逾期」标记**提示，标记常显。
     - **逾期置顶**：逾期事项归入当日列表顶部「⚠️ 逾期事项」分区（标注项数），按完成时间、优先级排序；**父待办存在逾期子任务时，父待办连同其全部子任务（含已完成的）整体归入该分区**，已完成子任务置灰且不可修改。
   - **日期视图**：
     - 待办归属日 = 完成时间的日期。历史归档页默认展示**当天**，提供 **‹ / › / 今天** 日期切换控件查询任意日期。
     - **呼出面板的待办页**具备全部能力（优先级/逾期/子任务/标签/备注/重复提醒），但**仅可查看当日待办，不提供日期切换**。
     - **日期与逾期展示口径**：归档页选择历史日期时，只展示归属于该日期的顶级待办，该日期未完成事项均标记为逾期；选择今天或呼出面板时，展示今天事项及此前日期仍未完成的逾期事项，逾期事项置于顶部，未来日期事项不提前进入列表。父待办存在逾期子任务时，父待办及其整棵子任务树进入今天的逾期区；历史日期详情仅展示归属于所选日期的事项。所有日期/时间按用户当前时区解释，跨午夜、夏令时和多设备场景必须使用带时区的时间值比较。
   - **提醒（v1.2）**：
     - **未设置提醒时间**（默认）：在**完成时间前 30 分钟、前 5 分钟、完成时间时刻**各提醒一次。
     - **设置了提醒时间**：**只在该提醒时刻提醒一次**（不再触发默认提醒）。
     - 卡片徽章：已设提醒 → 常显「⏰ 日期 时间」（与完成同日仅显时间），点击徽章打开编辑弹窗（提醒模式）修改；未设置 → 淡色 ⏰ 占位，悬浮提示默认提醒计划，点击可设置。
     - 到期时以**屏幕右上角弹出提醒卡片**形式展示（非系统通知弹窗），支持**直接关闭**或**下拉框选择下次提醒时间**。
     - **重复提醒**常显 🔁 徽章（每天/每周），点击弹出下拉修改循环方式。
       - v1.2 中“重复”只表示同一待办的提醒计划，不自动复制新待办；提醒未被完成或关闭时，按选定周期生成下一次提醒，完成或删除后停止。提醒卡片中的“下次提醒时间”只修改下一次 `remind_at`，不改变计划完成时间。调度器必须记录已触发的提醒实例，避免应用重启或每分钟轮询重复弹出同一实例。
   - **备注展示样式（v1.2，偏好设置可配）**：
     - `图标徽章 + 悬浮`：卡片徽章区显示 📄 图标，悬浮 tooltip 显示完整备注，点击进入编辑。
     - `置灰文本行`：备注以置灰单行文本显示在任务内容下方，超出一行省略号截断，悬浮显示全文，点击进入编辑。
     - `混合模式`（**默认**）：备注 ≤100 字（含）使用文本行；>100 字使用图标徽章。
   - **卡片按钮悬浮显隐（v1.2）**：
     - **✕ 删除按钮**：待办/子任务卡片**右上角**，仅占据一个角落位置，**仅悬浮该卡片自身内容区时显示**；徽章区预留角落空间避免重叠。
     - **悬浮层级隔离**：悬浮待办事项时，其子任务的操作按钮与删除按钮不展示；悬浮子任务时，父级待办的按钮不展示（同一时刻只有被悬浮层级的按钮可见，杜绝误触与显隐抖动）。
     - **底部一行**：**左侧**为标签 chips 与**完成时间徽章**（常显）——完成时间徽章紧跟标签之后，格式「📅 今天 HH:mm」或「📅 M/D HH:mm」，逾期时红色；**点击徽章弹出「修改完成时间」弹窗**（仅完成日期/时刻可编辑，其余字段锁定；子任务不能晚于父待办，保存后列表排序与逾期判定即时更新）；**右侧**为悬浮操作区：⏰ 提醒徽章、＋子任务（仅父级）、✏️ 编辑。
     - **子任务与父级同构**：子任务卡片同样具备左下角标签、完成时间徽章、备注展示与底部右侧操作区（⏰/✏️，无 ＋子任务），仅不显示折叠箭头对应的子任务树。
   - **修改与删除**：
     - 点击 ✕ 不直接删除：**卡片上方悬浮确认框**（「⚠️ 确认删除该待办事项/子任务？/ 删除 / 取消」）二次确认后执行；确认期间删除按钮保持常显。
     - **已完成的待办不允许修改、删除**（编辑/删除/优先级/标签/备注/提醒/完成时间均拦截并 toast 提示），**包括取消勾选恢复未完成**（点击已完成项的 checkbox 拦截提示）；唯一例外：已完成的**父待办**仍可 **＋子任务**——新建未完成子任务后，系统自动将父待办**恢复为未完成**（新子任务默认完成时间 = 1 小时后，不受"不晚于父级完成时间"约束，因已完成父级的完成时间必然已过）。
   - **子任务规则**：
     - 所有子任务完成后，父待办自动完成；子任务完成后排在子级列表末尾。
     - 树连接线（│ ├─ └─）、层级深度渐变、折叠/展开（折叠状态由箭头方向指示）。
     - 卡片不再显示「n/m 子任务」进度计数（v1.2 移除）。

### 2.3 状态栏常驻与开机启动（System Integration）
- **开机自启动**：支持设置开机自启动，静默启动（无弹窗、无闪烁、不激活主窗口）。
- **状态栏常驻**：
  - **macOS**：集成 `NSStatusBar`（Menu Extra），深色/浅色模式自适应图标。
  - **Windows**：集成任务栏托盘（System Tray），提供右键菜单。
  - **交互规范**：
    - 左键单击：直接打开 Inkling 单窗口（默认笔记视图）。
    - 右键单击：弹出 **Inkling 主菜单**（菜单标题为应用名+图标），含【📚 历史归档】【📊 统计报表】【⚙️ 偏好设置】【⏻ 退出】，分别打开单窗口的对应视图。
- **Inkling 单窗口（v1.2，左右结构）**：
  - **标题栏**：左侧固定展示**项目名称与图标**（✒️ Inkling），不随视图变化；右侧关闭按钮。
  - **左侧边栏**：笔记 📝 / 粘贴板 📋 / 待办 ✅ 三个页签；**底部左侧为 ⚙️ 偏好设置、右侧为 📊 统计数据**图标按钮；点击任意页签或图标，均在**右侧主内容区**展示对应数据，**不再弹出多个独立窗口**。
  - **当月迷你热力图（v1.2，侧边栏底部）**：
    - 展示**当前月份**的活跃度格子（周一对齐、按周分列），数据为**真实业务数据按日统计**（笔记/粘贴板/待办，含已完成与逾期数）；这里的“pin”仅指日期详情中的类别标识，不是“置顶”状态。
    - **悬浮**某日显示当日明细 tooltip（日期+笔记/复制项/待办条数、已完成/逾期，逾期红字，格子红框）。
    - **点击**某日 → 右侧主内容区切换为**日期详情视图**：标题为该日期，下方按**时间先后顺序**混排该日全部笔记、粘贴板条目、待办事项（**待办取完成时间**，子任务标注所属父待办，逾期标红），支持**按类别筛选**（全部/📝笔记/📋粘贴板/✅待办）；默认全部混排，**不同类别以不同颜色区分**（笔记=蓝色、粘贴板=黄色、待办=绿色左侧色条+徽章）。
    - **详情卡片（v1.2）**：每张卡片展示完整信息——待办含优先级徽章、提醒时刻、重复规则、标签、备注与逾期标记；笔记含 Markdown 渲染正文与标签；粘贴板含类型徽章与正文（超长省略号截断，悬浮看全文）。
    - **卡片操作（v1.2）**：**悬浮卡片右上角**显示 ✏️ 编辑 与 ✕ 删除——编辑分别走既有流程（笔记回显主面板编辑、粘贴板弹内容编辑浮框、待办弹编辑弹窗）；删除先在**卡片上方悬浮确认框**二次确认；**头部提供关键字搜索框**（匹配文本/标签/备注，与类别筛选叠加）；任何增删改后迷你热力图、归档列表与日期详情实时同步。
  - **侧边栏折叠与拖宽（v1.2）**：
    - 拖动侧边栏与内容区之间的分隔条可**实时调整宽度**（110~280px）。
    - 拖动宽度低于阈值（110px）时**自动折叠为图标窄栏**（52px，仅显示图标，页签与设置/统计仍可点击）。
    - 折叠/展开按钮（« / »）位于**分界线的垂直居中处**，**默认隐藏**，鼠标悬浮侧边栏或分界线时显示；点击在折叠 ↔ **展开恢复默认宽度 150px** 间切换。
  - **主内容区滚动**：内容区独立滚动，窗口过小时数据完整可查。

### 2.4 使用程度统计（Activity & Trends，v1.2 重做）

> **统计口径（冻结前置条件）**：活跃度不是“置顶 pin 数”。笔记按“成功归档”计数，粘贴板按“成功捕获的新条目”计数，待办分别记录“创建”和“实际完成”事件；热力图中的待办总数按计划完成日期统计，已完成数按 `completed_at` 统计，逾期数按查询时刻由未完成状态与计划完成时间计算。统计写入必须幂等，应用重启、重复事件或编辑事项不能重复累加。
- 统计视图内嵌于历史归档单窗口的主内容区（侧边栏底部 📊 进入）。
- **每日活跃度热力图**：
  - GitHub 风格日历格子（列=周、行=星期一~星期日），支持横向滚动。
  - **顶部标注每个月的范围**（月份标签与周列对齐）；左侧标注星期（一/四/日）。
  - 格子颜色深浅表示当日活跃度（笔记+复制项+待办总量分档）。
  - **悬浮明细**：悬浮任意格子显示 tooltip——日期+星期、📝 笔记条数、📋 复制项条数、✅ 待办条数（已完成多少、逾期多少；逾期数红色高亮）；**存在逾期的日期格子以红色边框标识**（tooltip 同步红边）。
  - 图例：活跃度色阶 + 红框=存在逾期。
- **每月趋势图**：**折线图**呈现近 6 个月各模块（笔记/粘贴板/待办）使用量走势，带图例、Y 轴刻度与月份标签，数据点悬浮显示数值。

### 2.5 桌面置顶小浮窗（Pin Mechanism）
- 任意一条笔记或待办可点击 `Pin` 按钮脱离主面板。
- 生成一个独立小浮窗常驻桌面角落（默认右下角，可自由拖拽吸附）。
- 浮窗特性：窗口级 `alwaysOnTop`；极简呈现（标题/首行 + 前 50 字）；透明度快捷调节（$30\% \sim 100\%$）；双击浮窗展开即时编辑，修改实时同步回本地数据库或文件。

### 2.6 数据持久化与导出
- **本地存储**：统一采用本地 SQLite（支持 WAL 模式），包含笔记、剪贴板、待办、标签关联、统计和设置表；表名与架构文档保持一致。
  - 待办表字段（v1.2）：内容、带时区的计划完成时间、实际完成时间（可空）、下一次提醒时间（可空）、提醒计划/重复规则、优先级、完成状态、父级引用、更新时间；标签和备注分别通过标签关联及备注字段存储。
  - **草稿归档一致性**：自动保存的草稿必须有稳定的草稿 ID；归档成功后在同一事务/可恢复流程中将草稿提升为正式笔记，失败不得出现“列表已归档但正文丢失”或“同一内容重复归档”。
- **超大笔记落盘路径**：`notes` 目录位于应用数据目录下，以 `.md` 纯文本存储；不得默认写入项目安装目录或项目源码目录。用户选择自定义目录时必须显式授权，并在路径不可用时回退到应用数据目录。
- **导出能力**：支持单条或批量导出为 Markdown / TXT / PDF / 图片 / 网页多种格式。

### 2.7 偏好设置（v1.2 增补）
- 失焦自动收起策略、粘贴板保留天数、开机静默自启动、全局快捷键录制。
- **备注展示样式**：`混合模式（默认）` / `图标徽章 + 悬浮` / `置灰文本行`，即时生效于所有待办卡片。
- **多主题换肤（v1.2）**：
  - 内置 **10 套主题**：深色（默认）、浅色、纸杯蛋糕、大黄蜂、翡翠绿、商务蓝、霓虹未来、复古、浪漫、万圣节。
  - 设置页以**色点预览芯片网格**呈现（每枚芯片带 4 色预览点），点击即时切换全站配色——背景、玻璃拟态面板、文字、强调色、优先级/类别色、热力图与折线图等所有图形同步换肤。
  - 选择通过 **localStorage 持久化**，刷新后保持。
  - 技术约束：全部界面颜色经由 CSS 主题令牌（`--wsa` 中性色基 + 语义色变量）驱动，新增界面元素必须使用既有令牌，禁止硬编码颜色。
- 全部下拉框选项**跟随当前主题**（`color-scheme` 随主题切换），不得出现白底不可读。

---

## 3. 开发优先级规划（Milestones）

| 阶段 | 交付目标 | 核心范围 |
| :--- | :--- | :--- |
| **P0 (MVP)** | 极速捕获闭环 | 顶部悬浮展开 + 笔记模式 + 即时渲染 + 按钮归档与 SQLite 暂存 + 状态栏常驻 + 全局快捷键 + 开机静默自启 |
| **P1** | 三态全能捕获 | 粘贴板历史（SQLite 存储 / 双击置顶粘贴 / 搜索 / 收藏 / 修改 / 两行省略） + 待办（完成时间模型 / 提醒计划 / 标签 / 备注 / 优先级阶梯 / 逾期置顶 / 子任务树） |
| **P2** | 深度桌面集成 | 桌面置顶浮窗 + 笔记 >1MB 自动落盘应用数据目录 `notes/` + 热力图与折线趋势 + 数据导出 + 历史归档单窗口（侧边栏折叠/拖宽/搜索） |
| **P3** | 进阶与扩展 | 动画组件渲染美化 + 云端 WebDAV 同步 + 多套极简毛玻璃主题 + 插件化能力 |
